Eth-Docker： 基于 Docker 容器化部署，在单主机（124 GB 内存）上构建 12 个自治系统（AS 101–112），每 AS 包含 3 个内部子网与 9 个以太坊 PoS 节点，合计 108 个节点（其中 107 个参与评估）。各 AS 通过 BIRD 软路由实现 BGP/OSPF 广域路由，并经由 4 个互联交换节点（ix51–54）形成层级 Mesh 拓扑，有效模拟多 AS 跨域路由环境与区域间传播延迟。攻击注入采用双层机制：链路层以 ip link set down 实现节点断连与隔离，网络层以 tc netem 施加可配置延迟、抖动、丢包与带宽限制，攻击结束后自动恢复网络状态。拓扑数据由部署于每个节点容器内的监控代理每 30 s 采集一次，分别通过 Geth admin_peers JSON-RPC 与 Lighthouse /eth/v1/node/peers API 获取执行层与共识层的对等连接信息，汇聚至中心采集器后写入 Neo4j 图数据库，支持拓扑历史的持久化存储与查询。代码在https://github.com/ht1220/ethereum_Virtual_Network.git；服务器在：ssh ubuntu@111.230.44.107 -p 22 User：ubuntu pwd：592087469hW!@

Eth-Cloud（主实验）： 100 台腾讯云虚拟机（4 核 CPU、8 GB RAM、50 GB SSD）部署于北京（25 节点，主枢纽）、南京、广州、成都、香港、新加坡与法兰克福共 7 区域 3 大洲，国内节点间延迟约 20–60 ms，跨洲延迟可达 200 ms，形成与真实以太坊主网相近的延迟异质性。每节点同时运行执行层（Geth v1.17.1）与共识层（Lighthouse v8.1.3），100 个节点各承载 1 个验证者，实现全员参与 PoS 共识；协议参数遵循 Deneb 分叉规范，链 ID 20250327，出块间隔 12 s，32 slots/epoch。节点间通过 JWT 认证保障执行层与共识层的引擎 API 通信安全，并采用 admin.addPeer() 手动建立对等连接，确保网络拓扑处于完全受控状态，不与以太坊主网混接。每节点最多维持 99 条对等连接（maxpeers=99）。控制节点：ssh ubuntu@49.233.22.103 -p 22，pdw：592087469hW!@

Pol-Docker： 基于 Substrate 框架，采用 BABE/GRANDPA 双层共识机制。网络跨 10 个自治系统（AS 201–210）部署，每 AS 统一配置 1 个归档节点、6 个验证者节点与 3 个全节点，合计 100 个节点；各 AS 配备内部路由器与边界路由器，通过 BIRD BGP 协议实现跨 AS 互联，并以 tc 流量控制模拟广域网环境（延迟 11–192 ms，抖动 7–45 ms，丢包 0.26–2.81%）。100 个节点均可正常建立跨域连接。拓扑采集由部署于每个节点容器内的监控代理承担，每 20 s 通过 Substrate system_peers RPC 查询对等连接列表，解析 multiaddr 格式提取 IPv4 地址后，按 IP 前缀区分本地 AS 内部连接与跨 AS 连接，并依据容器命名规则推断节点角色（验证者、归档节点或全节点），最终将拓扑快照写入 Neo4j 图数据库。代码在https://github.com/ht1220/polkadot_Virtual_Network.git；服务器在：ssh ubuntu@111.230.44.107 -p 22 User：ubuntu pwd：592087469hW!@

BTC-Docker： 运行 Bitcoin Core 27.0（regtest 模式），跨 12 个自治系统（AS 171–182）部署 108 个节点，每 AS 含 3 个内部子网，各子网 3 个节点；通过 3 个互联交换节点（IX71–73）与 1 个transit AS（AS4）实现跨域路由，以 BIRD2 BGP/OSPF 模拟广域网多 AS 拓扑。4 个矿工节点分散于不同 AS，主矿工预挖 150 个区块以保证币龄成熟，其余矿工以 30 s、45 s、60 s 的错峰间隔出块，模拟现实矿池分布格局；每节点维持 25–43 条 P2P 对等连接（maxconnections=125），并禁用 DNS 种子，仅通过 addnode 在仿真网络内部完成对等发现，确保与公网完全隔离。实验支持四类网络攻击：以 net_down/bird_peer_down 实施日蚀攻击、以 BIRD 配置注入实施 BGP 路由劫持、以停止 transit 路由器容器实施网络分区、以 tc netem（500 ms 延迟 + 30% 丢包）模拟区块传播受阻。拓扑采集由中心监控服务（btc_monitor）承担，每 10 s 通过 getpeerinfo JSON-RPC 轮询全部 108 个节点，提取对等连接地址后写入 Neo4j 图数据库（以 BtcNode 实体和 PEERS_WITH 边记录连接及流量字节数），并同步检测跨节点的链分叉事件。部署的云服务器ssh ubuntu@106.52.91.185 -p 22 pwd: 592087469hW!@，它的代码：https://github.com/ht1220/bitcoin_Virtual_Network.git

Hyperledger Fabric-Docker：基于 Hyperledger Fabric 2.5 联盟链框架，采用三层 AS 层级拓扑部署 108 个节点：核心层 AS 4 配备 4 台路由器经 IBGP/OSPF 互联 4 个 IXP；区域传输层 AS 41–44 各驻 1 个 IXP；客户层 AS 201–212 对应 12 个组织（Org1–Org12），每组织部署 8–9 个 Peer 节点，合计 103 个 Peer 与 5 个分布于 AS 201–205 的 Raft Orderer，运行容器共 149 个。共识采用 Raft 协议，单通道 `fabricchannel`，链码为 Go 语言实现的资产转移合约（40% 创建、25% 转移、25% 更新、10% 查询）。各 AS 通过 BIRD BGP 协议实现跨域路由，内部以 OSPF 路由；以 `tc netem` 施加可配置延迟（1–300 ms）、带宽限制（5 Mbit–1 Gbit）与丢包（0–5%），并预置 LAN/城域/区域/全球四种网络场景。监测栈包含 Prometheus 指标采集、Grafana 可视化仪表盘、PostgreSQL 持久化存储与自研 Fabric Monitor 健康服务，覆盖共识/背书/网络三层指标；Gossip 协议通过 Leader 选举与 Anchor 节点协调跨 AS 对等发现。在云服务器ssh ubuntu@119.91.51.36 -p 22 pwd：592087469hW!@，部署了108个节点虚拟化广域网的Hyperledger Fabric网络，它的代码：https://github.com/ht1220/Hyperledger_Fabric_Virtual_Network.git