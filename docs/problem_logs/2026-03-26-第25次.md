# 2026-03-26-第25次

## 变更

- 按要求先记录后执行：在已有 fair-time 基础上补 `fair-edge` 口径。
- 已在 `src/models/baselines.py` 增加 `edge_change_budget` 约束参数（四个基线统一支持）。
- 已在 `experiments/run_eth_real_experiments.py` 新增 `5c. Fair-Edge Baseline Comparison`，并写入 `comparison_fair_edge`。

## 实验命令

```bash
.venv/bin/python experiments/run_repro_tifs.py --eth
```

## 关键结果

- 运行成功：`exit=0`，总耗时约 `1441.5s`（ETH 单网）。
- Ours 主结果保持：`R=0.7981`（`+44.38%`）。
- `5b Fair-Time`（time budget=88.1s）：
  - ResiNet `R=0.8069`（`+45.96%`，edge_changes=1720）
  - Static `R=0.7474`（`+35.20%`，edge_changes=964）
  - AttackSim `R=0.7228`（`+30.75%`，edge_changes=1092）
- `5c Fair-Edge`（edge budget=1616）：
  - ResiNet `R=0.8101`（`+46.55%`，edge_changes=1616，time=111.9s）
  - Static `R=0.7474`（`+35.20%`，edge_changes=964，time=13.8s）
  - AttackSim `R=0.7737`（`+39.97%`，edge_changes=1616，time=588.3s）
  - FPSblo-EP `R=0.6477`（`+17.16%`，edge_changes=18，time=1.5s）

## 结论与下一步

- ETH 公平性口径已从单一 fair-time 扩展到 fair-edge，证据更完整。
- 结果显示：在 ETH 上，ResiNet 在公平预算口径下可超过 Ours（至少在 R 指标上），后续论文需并列报告多口径结果。
- 下一步：将 `fair-edge` 同步到 `run_real_dual_network_experiments.py` 并重跑 `--dual`，补齐 DOT 的公平预算证据链。

