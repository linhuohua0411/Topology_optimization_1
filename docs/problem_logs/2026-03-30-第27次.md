# 2026-03-30-第27次

## 变更

- 将 fair-edge 最小预算下限继续上调，避免预算退化：
  - `experiments/run_eth_real_experiments.py`
  - `experiments/run_real_dual_network_experiments.py`
- 规则从 `edge_budget = max(1, ours_changes)` 调整为 `edge_budget = max(10, ours_changes)`。

## 实验命令

```bash
.venv/bin/python experiments/run_repro_tifs.py --dual
```

## 关键结果

- 已启动重跑命令：`.venv/bin/python experiments/run_repro_tifs.py --dual`
- 当前状态：运行中（长任务，仍在执行）；完成后回填最终数值。

## 结论与下一步

- 已按计划把最小预算下限提升到 `10` 并启动双网复测。
- 待复测完成后重点核对：DOT 的 `5c fair-edge` 是否仍停留在 0 改边与 0 增益。

