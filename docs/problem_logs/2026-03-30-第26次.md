# 2026-03-30-第26次

## 变更

- 执行“下一步”：将 `fair-edge` 口径同步到双网实验脚本。
- 代码整理：抽取双网脚本中的基线评测重复逻辑，减少冗余。

## 改动文件

- `experiments/run_real_dual_network_experiments.py`
  - 新增 `_count_edge_changes` 与 `_run_baseline_suite`，统一基线评测流程
  - 新增 `5c. Fair-Edge Baseline Comparison`
  - 保留并复用 `5b. Fair-Time`，打印格式统一

## 实验命令

```bash
.venv/bin/python experiments/run_repro_tifs.py --dual
```

## 关键结果

- 运行成功：`exit=0`，双网总耗时约 `5478s`（`91.3min`）。
- 产物更新：`experiments/metrics/dual_network_results.json` 与 `fig_dual_*` 全部落盘。

### ETH（106 节点）

- 主流程：Ours `R=0.7981`（`+44.38%`）
- `5b fair-time`（time budget=88.3s）：
  - ResiNet `+45.96%`，Static `+35.20%`，AttackSim `+30.75%`，FPSblo-EP `+17.16%`
- `5c fair-edge`（edge budget=1616）：
  - ResiNet `R=0.8101`（`+46.55%`，111.9s）
  - AttackSim `R=0.7737`（`+39.97%`，589.5s）
  - Static `R=0.7474`（`+35.20%`，13.8s）
  - FPSblo-EP `R=0.6477`（`+17.16%`，1.5s）

### DOT（100 节点）

- 主流程：Ours `R=0.7607`（`+0.00%`，稳定性 `0.00%±0.00%`）
- `5b fair-time`（time budget=73.8s）：
  - ResiNet `+11.55%`，Static `+7.08%`，AttackSim `+2.51%`，FPSblo-EP `+0.06%`
- `5c fair-edge`（edge budget=0）：
  - 全方法均 `+0.00%`（预算为 0 导致不可改边）
- failure mode：`21` 组有增益、`6` 组无增益（规律与此前一致：`k_max>=100` 约 `+4.94%~+4.98%`）。

## 结论与下一步

- 双网脚本已具备 `default + fair-time + fair-edge` 三口径输出。
- 代码重复已明显降低：基线评测逻辑统一由 `_run_baseline_suite` 管理，维护成本下降。
- 需补充说明：DOT 的 fair-edge 目前预算为 0（因 Ours 改边数为 0），建议下一步加入“最小预算下限”（如 `max(1, ours_changes)`）或改用“target-improvement budget”避免退化口径。

